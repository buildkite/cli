package main

var (
	// Version is the version of the CLI tool
	Version = "0.0.1"

	// BuildNumber is set at compile time and reflects the Buildkite Build Number
	BuildNumber = ""

	// GitCommit is set at compile time and reflects the git commit at build time
	GitCommit = ""
)
